// Size of Array
#include<stdio.h>
main()
{
    char name[10];
    int a[20];
    double b[20];
    float c[10];

    printf("\nMemory Occupied by float is %d and by Character is %d and by Array a is %d and by double is %d",sizeof(c),sizeof(name),sizeof(a),sizeof(b));
    printf("\n");
}

